export interface UnTaggedCase {
    lineNumber: string;
    itemNumber: string;
    caseCount: string;
  }

  export interface LotTagLits {
    lineNumber: string;
    itemNumber: string;
    date: string;
    uniqueNo:string;
    lotNumber:string;
  }